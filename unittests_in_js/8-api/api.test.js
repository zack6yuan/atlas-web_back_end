const chai = require('chai')
const request = require('request')
const { expect } = chai;
const api = require('./8-api/api')
describe('api', function() {
    it('should return a status code of 200', function() {
        .get('/')
    })
})