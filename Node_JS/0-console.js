#!/usr/bin/env node
// function displayMessage that prints in STDOUT the string argument
function displayMessage(arg) {
    // log to the console the argument passed
    console.log(arg);
}

// export the module
module.exports = displayMessage;